# config

Git config file with useful aliases, enjoy.

Files:
- `gitconfig template`: documented git alias template, including the worktree aliases.
- `worktree-functions.zsh`: `zsh` worktree helpers (`gwtl`, `gwtp`, `gwtco`, `gwtrm`) plus navigation helpers for moving between the main checkout and sibling worktrees.
