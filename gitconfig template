# This is Git's per-user configuration file.
[user]
	name = 
	email = 
[alias]

  ##################################### switch branch
	co = checkout

  ##################################### amend last commit
	amd = commit --amend

  ##################################### nicely log last 10 commits to console
	lg = log -n 10 --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit

  ##################################### create empty commit
	empty = commit --allow-empty -m'+semver: minor'

  ##################################### discard current changes
	dsd = checkout -- .

  ##################################### push to origin for the first time, without typing set upstream option
	pusho = "!git push -u origin \"$(git rev-parse --abbrev-ref HEAD)\""

  ##################################### fetch master without changing branch
	fm = "!git fetch origin master:master"

  ##################################### log graph for all branches
	lgr = log --pretty=oneline --graph --decorate --all
	unset = branch --unset-upstream
	fo = "!f() { git fetch origin $1:$1; }; f"
	current = rev-parse --abbrev-ref HEAD
	rename = "!f() { git branch -m $1; }; f"
	repos = "C:\\workspace\\source"
	ignore = "!f() { git update-index --assume-unchanged $1; }; f"
	unignore = "!f() { git update-index --no-assume-unchanged $1; }; f"
	ignored = !git ls-files -v | grep ^[a-z]

  ##################################### shorthand for git worktree
	wt = worktree

  ##################################### list worktrees
	wtl = worktree list

  ##################################### prune stale worktree metadata
	wtp = worktree prune

  ##################################### remove a matching worktree under ../<repo>-worktrees/<branch>
	wtr = "!f() { repo=$(basename \"$(git rev-parse --show-toplevel)\"); git worktree remove \"../${repo}-worktrees/$1\"; }; f"

  ##################################### add a worktree for a local branch, or for an already-fetched origin/<branch>
	wtb = "!f() { repo=$(basename \"$(git rev-parse --show-toplevel)\"); branch=\"$1\"; path=\"../${repo}-worktrees/${branch}\"; if git show-ref --verify --quiet \"refs/heads/${branch}\"; then git worktree add \"$path\" \"$branch\"; elif git show-ref --verify --quiet \"refs/remotes/origin/${branch}\"; then git worktree add -b \"$branch\" \"$path\" \"origin/$branch\"; else printf 'wtb: branch %s not found locally or in refs/remotes/origin; fetch first if needed\n' \"$branch\" >&2; return 1; fi; }; f"

  ##################################### create a new branch and matching worktree under ../<repo>-worktrees/<branch>
	wtbn = "!f() { repo=$(basename \"$(git rev-parse --show-toplevel)\"); git worktree add -b \"$1\" \"../${repo}-worktrees/$1\" \"${2:-origin/main}\"; }; f"
[core]

  ##################################### use notepad++ as default editor
  ##	editor = "\"C:\\Program Files\\Notepad++\\notepad++.exe\" -multiInst -notabbar -nosession -noPlugin %*"

  ##################################### use vs code as default editor
	editor = code -n --wait
[diff]
    tool = default-difftool
[difftool "default-difftool"]
    cmd = code --wait --diff $LOCAL $REMOTE
[merge]
    tool = vscode
[mergetool "vscode"]
    cmd = code -n --wait $MERGED
[credential]
	helper = wincred


############################################################# LATEST VERSION

# This is Git's per-user configuration file.
[user]
	name = 
	email = 
[alias]
	co = checkout
	amd = commit --amend
	lg = log -n 10 --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
	empty = commit --allow-empty -m'+semver: minor'
	dsd = checkout -- .
	pusho = "!git push -u origin \"$(git rev-parse --abbrev-ref HEAD)\""
	fm = "!git fetch origin master:master"
	lgr = log --pretty=oneline --graph --decorate --all
	unset = branch --unset-upstream
	fo = "!f() { git fetch origin $1:$1; }; f"
	current = rev-parse --abbrev-ref HEAD
	rename = "!f() { git branch -m $1; }; f"
	repos = "C:\\workspace\\source"
	ignore = "!f() { git update-index --assume-unchanged $1; }; f"
	unignore = "!f() { git update-index --no-assume-unchanged $1; }; f"
	ignored = !git ls-files -v | grep ^[a-z]
	wt = worktree
	wtl = worktree list
	wtp = worktree prune
	wtr = "!f() { repo=$(basename \"$(git rev-parse --show-toplevel)\"); git worktree remove \"../${repo}-worktrees/$1\"; }; f"
	wtb = "!f() { repo=$(basename \"$(git rev-parse --show-toplevel)\"); branch=\"$1\"; path=\"../${repo}-worktrees/${branch}\"; if git show-ref --verify --quiet \"refs/heads/${branch}\"; then git worktree add \"$path\" \"$branch\"; elif git show-ref --verify --quiet \"refs/remotes/origin/${branch}\"; then git worktree add -b \"$branch\" \"$path\" \"origin/$branch\"; else printf 'wtb: branch %s not found locally or in refs/remotes/origin; fetch first if needed\n' \"$branch\" >&2; return 1; fi; }; f"
	wtbn = "!f() { repo=$(basename \"$(git rev-parse --show-toplevel)\"); git worktree add -b \"$1\" \"../${repo}-worktrees/$1\" \"${2:-origin/main}\"; }; f"
[core]
	# editor = "\"C:\\Program Files\\Notepad++\\notepad++.exe\" -multiInst -notabbar -nosession -noPlugin %*"
	# editor = "\"C:\\Program Files\\Microsoft VS Code\\Code.exe\" -nosession --wait %*"
	editor = code -n --wait
#[diff]
#    tool = default-difftool
#[difftool "default-difftool"]
#    cmd = code --wait --diff $LOCAL $REMOTE
[diff]
    tool = vscode
[difftool "vscode"]
    #cmd = code -nosession --wait --diff $LOCAL $REMOTE
	cmd = code -n --wait --diff $LOCAL $REMOTE
[merge]
    tool = vscode
[mergetool "vscode"]
    cmd = code -n --wait $MERGED
[credential]
	helper = wincred
[mergetool "sourcetree"]
	cmd = "'' "
	trustExitCode = true
[winUpdater]
	recentlySeenVersion = 2.19.1.windows.1
